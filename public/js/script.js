function setLogoColor() {
    
    if( document.querySelector('.inner-nav .logo-wrapper h1') != null ) {
        const logo = document.querySelector('.inner-nav .logo-wrapper h1');
        console.log(logo.innerText);
    }
}